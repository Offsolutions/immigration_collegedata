@model IEnumerable<NewImmigration.Areas.Admin.Models.Campus>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/LayoutPage.cshtml";
}

<h2><b>Index</b></h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.College.CollegeName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CampusName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Distance)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.College.CollegeName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CampusName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Distance)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CampusId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CampusId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CampusId })
        </td>
    </tr>
}

</table>
